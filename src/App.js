import logo from "./logo.svg";
import "./App.css";

const App = () => {
  const [count, setCount] = useState(10);

  return (
    <div>
      <button>+</button>
    </div>
  );
};

export default App;
